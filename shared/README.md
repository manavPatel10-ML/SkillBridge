# Shared Domain Contracts & Utilities

**Owner**: Shared (Both Developer 1 and Developer 2)

This directory houses the single source of truth for:
- TypeScript interfaces & domain models (`types/`)
- Canonical learning, role, and problem catalog (`constants/content-catalog/`)
- Validation schemas (`schemas/`)
- Pure utility functions & contracts (`utils/`)

All modifications to files in `shared/` require review and agreement between both developers.
